[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python main.py bot & python -m scheduler.jobs"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[env]
TZ = "Asia/Jakarta"
PYTHON_VERSION = "3.11"
